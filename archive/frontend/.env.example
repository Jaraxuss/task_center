# 开发环境下推荐不设置 VITE_API_BASE_URL，前端会直接请求相对路径 /api/*，
# 再由 Vite dev server 代理到真实后端，避免浏览器直连 8000 产生 CORS。
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# 如需在非 dev 场景显式指定 API 基地址，可取消注释：
# VITE_API_BASE_URL=http://127.0.0.1:8000
