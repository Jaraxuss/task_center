# Development / test dependencies for the backend.
# Install on top of requirements.txt:
#   .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
ruff>=0.6,<1.0
mypy>=1.10,<2.0
alembic>=1.13,<2.0
